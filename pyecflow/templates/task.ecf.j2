{{ TASK_RESOURCES }}

model=%MODEL%

%include <head.h>
%include <envir-p1.h>

{{ TASK_MODULES }}
module list

{{ TASK_VARIABLES }}

{{ TASK_SCRIPT }}
if [[ $? -ne 0 ]]; then
   ecflow_client --msg="***JOB ${ECF_NAME} ERROR RUNNING J-SCRIPT ***"
   ecflow_client --abort
   exit
fi

%include <tail.h>

%manual
{{ TASK_MANUAL }}
%end
